<%#
name: trewa
snippet: false
model: Ptable
oses:
- AlmaLinux
- CentOS
- CentOS_Stream
- Fedora
- RedHat
- Rocky
%>
#Dynamic


BOOT_VG="boot_$(hostname -s)"
DOCKER_VG="docker_$(hostname -s)"

BOOT_DEV=sda

cat <<EOF > /tmp/diskpart.cfg
ignoredisk --only-use=${BOOT_DEV}
zerombr
clearpart --drives=${BOOT_DEV} --all --initlabel

part /boot     --size=1024 --asprimary --ondrive=${BOOT_DEV}
part /boot/efi --size=200  --asprimary --ondrive=${BOOT_DEV} --fstype=efi

part pv.boot   --size=102400 --ondisk=${BOOT_DEV}
part pv.docker --size=1 --grow --ondisk=${BOOT_DEV}

volgroup ${BOOT_VG} pv.boot
volgroup ${DOCKER_VG} pv.docker

logvol /               --vgname=${BOOT_VG} --size=1 --grow --name=root
logvol /var/lib/docker --vgname=${DOCKER_VG} --size=1 --grow --name=docker
EOF
